<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.opinius">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TambahSurveyActivity" />
        <activity android:name=".TipeJawabActivity" />
        <activity android:name=".QuestionForm.FormIsianActivity" />
        <activity android:name=".QuestionForm.FormSatuJawabanActivity" />
        <activity android:name=".QuestionForm.FormBanyakJawabanActivity" />
        <activity android:name=".survey_terjawabActivity" />
        <activity android:name=".SurveiTerjawabActivity"></activity>
    </application>

</manifest>